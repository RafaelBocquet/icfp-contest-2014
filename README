Readme
======

ICFP Submission

Team Piter

Rafaël Bocquet & Simon Mauras

 * submission is the OUTPUT file
 * The ICFP folder contains haskell code, with a cabal project file
 * Lambda Man files in LM
 * Ghost files in GH
 * Compilation : 
    cd ICFP
    cabal sandbox init
    cabal install --only-dependencies
    cabal build
    dist/build/ICFP/ICFP pacman test.hl
    # Creates a test.hl.out file